publishMods {
    file = remapJar.archiveFile

    modLoaders.add("fabric")
    dryRun = debug_mode.toBoolean()

    curseforge {
        requires("fabric-api")
    }
    modrinth {
        requires("fabric-api")
    }
}